#!/bin/bash

echo "STEP 1: build_step1.sh from version 7 is taking over the default one."

# this script will be executed, as long as no other build_step1.sh be found inside sub version or modification folder tree
